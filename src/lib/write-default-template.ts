export const DEFAULT_PRODUCT_TITLE = 'TV ✅ 이메일 셀프인증 ✅ 프리미엄 ✅';

export const DEFAULT_PRODUCT_DESCRIPTION = `TV ✅ 이메일 셀프인증 ✅ 프리미엄 ✅

⚠️ 1 1 1 원칙을 꼭 지켜주세요 ⚠️
1인 1기기 1계정 원칙이며 어길 시 약정에 의거 위약금 부과됩니다!`;

export function makeDefaultProductTitle(_serviceLabel?: string): string {
  return DEFAULT_PRODUCT_TITLE;
}

export function makeDefaultProductDescription(_serviceLabel?: string): string {
  return DEFAULT_PRODUCT_DESCRIPTION;
}
